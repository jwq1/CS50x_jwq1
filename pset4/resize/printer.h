#include <bmp.h>
#include <stdlib.h>



// Create a function that will store the the value of pixels in a row, 
    // This saved memory, will then be used to recursively print the row 'n' times.

void saveRow(n, ) {
    
    // n should be the factor we collected. Maybe we can point to that piece of memory.
    
    // 
    
};