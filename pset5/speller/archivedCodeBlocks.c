




 // // INCOMPLETE ATTEMPT AT RECURSION
    // int n = HASHTABLE_SIZE;
    // node *cursor = hashtable[]

    // void recurUnload(n) {

    //     if (hashtable[n] == NULL && ) {
    //         free(hashtable[n])
    //         return 1;
    //     }
    //     else {

    //         return n * recurUnload(n - 1);
    //     }


    // }

    // // INCOMPLETE ATTEMPT AT RECURSION 1
    // // Make sure no more elements or nodes are in hashtable
    //     // Check whether next element equals null.
    //     if (hashtable[i + 1] != null) {

    //         // If true, call unload
    //         unload();
    //     }
    //     // check whether new_node->next != null
    //     else if (hashtable[])



    //         // If true, call unload

    //     // Else, there must not be any elements or nodes left in the table
    //         // Free the node



